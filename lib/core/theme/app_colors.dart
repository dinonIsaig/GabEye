import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Brand Colors
  static const Color primaryColor   = Color(0xFF2C4670);
  static const Color darkMode = Color(0xFF12161C);

  // Text Colors
  static const Color textPrimary = Color(0xFF12161C);
  static const Color textSecondary = Color(0xFF2B343F);
  
  // Semantic Colors
  static const Color errorRed = Color(0xFFDC2626);
  static const Color successGreen = Color(0xFF16A34A);
  
  // UI Elements
  static const Color disabledButton = Color(0xFFE5E7EB);
  static const Color disabledText = Color(0xFF6B7684);

  //Button Colors
  static const Color primaryButton   = Color(0xFF14395F);
}